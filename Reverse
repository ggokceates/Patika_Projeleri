liste = [[1, 2], [3, 4], [5, 6, 7]]
remlist = []


def reverse(x):
    x = list(reversed(x))
    for i in x:
        if type(i) == list:
            remlist.append(list(reversed(i)))


reverse(liste)
print(remlist)
